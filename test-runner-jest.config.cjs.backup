const { getJestConfig } = require('@storybook/test-runner');

/**
 * @type {import('@jest/types').Config.InitialOptions}
 */
module.exports = {
  // Use the default test runner config without modifications
  ...getJestConfig(),
};
