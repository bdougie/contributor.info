# Palette's Journal

## 2024-05-22 - Initial Setup
**Learning:** This repo uses `npm` (package-lock.json) instead of `pnpm` as suggested in the prompt examples.
**Action:** Use `npm` for all package management commands.
