{
  "importMap": "./import_map.json",
  "tasks": {
    "test": "deno test --allow-all --coverage=coverage _shared/ tests/ __tests__/ spam-detection/",
    "test:watch": "deno test --allow-all --watch",
    "coverage": "deno coverage coverage --lcov --output=coverage/lcov.info",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-explicit-any", "no-unused-vars", "require-await"]
    },
    "include": [
      "_shared/**/*.ts",
      "tests/**/*.ts",
      "__tests__/**/*.ts",
      "spam-detection/**/*.ts",
      "health/index.ts"
    ]
  },
  "fmt": {
    "indentWidth": 2,
    "lineWidth": 100,
    "semiColons": true,
    "singleQuote": true
  }
}