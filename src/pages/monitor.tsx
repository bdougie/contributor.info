import { CaptureHealthMonitor } from '@/components/CaptureHealthMonitor';

export default function MonitorPage() {
  return (
    <div className="container mx-auto px-4 py-8">
      <CaptureHealthMonitor />
    </div>
  );
}
