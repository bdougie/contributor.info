/**
 * Chart components - re-exports from original implementation
 * 
 * For lazy-loaded recharts components, components should import directly
 * from 'recharts' and wrap them in Suspense boundaries.
 */

// Re-export everything from the original chart implementation
export * from './chart-original';